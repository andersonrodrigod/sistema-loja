<div class="container-caminho-links">
    <h1>Anos</h1>
    <div class="caminho-links"> 
        <ul class="list-links">
        {{#each year}}
                <li class="btn-links">
                    <a href="/producers/all/{{this.id}}/{{this.name}}">{{this.name}}</a>
                </li>
        {{/each }}
            
        </ul>
    </div>
</div>