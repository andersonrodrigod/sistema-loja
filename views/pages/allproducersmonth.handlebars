<div class="container-caminho-links">
    <h1>Meses</h1>
    <div class="caminho-links">
        <ul class="list-links">
        {{#each month}}
            <li class="btn-links"><a href="/producers/all/{{this.id}}/{{this.year}}/{{this.month}}">{{this.name}}</a></li>
        {{/each}}
        </ul>
    </div>
</div>