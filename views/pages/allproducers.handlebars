<div class="container-caminho-links">
    <h1>Produtoras</h1>
    <div class="caminho-links">
        <ul class="list-links">
            {{#each producers}}
                <li class="btn-links">
                    <a href="/producers/all/{{this.id}}">{{this.name}}</a>
                </li>
            {{/each }}
        </ul>
    </div>
</div>