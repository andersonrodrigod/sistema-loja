<div class="container-caminho-links">
    <h1>Vendas Anual</h1>
    <div class="caminho-links">
        <ul class="list-links">
        {{#each sales}}
            <li class="btn-links">
                <a href="/sales/salesyear/{{this.year}}">{{this.year}}</a>
            </li>
        {{/each }}
        </ul>
    </div>
</div>

